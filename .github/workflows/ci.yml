name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --

      - name: Check
        run: cargo check

      - name: Build examples
        run: |
          cargo build --example decode_rds_spy_log --release
          cargo build --example rds_viewer --release

      - name: Tests
        run: cargo test

      - name: Docs
        run: cargo doc --no-deps